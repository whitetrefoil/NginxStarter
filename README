这是一个自学.Net/WPF编程而新建的项目，作为Windows下启动、关闭Nginx的一个GUI，基本没有实际意义。

Changelog:

0.0.2.2

Fixed: 解决了在启动Nginx之前点“强制退出”按钮导致崩溃的问题。
Dev Info: 纠正了Nginx.cs文件中quit()和stop()方法名混乱的问题，现在方法名、按钮名、nginx自身命令行中quit、stop的含义均对应了

----------

0.0.2.1

Added: 加入了CoffeeScript、LESS、Sass的标签页，完成了CoffeeScript标签页的样式设计，但是均没有功能无法使用
Added: 启动Nginx之前“重启”、“退出”等按钮（包括任务栏图标菜单）将被禁用，启动Nginx后“启动”、“浏览”等按钮会被禁用
Added: 强制退出Nginx后追加杀死所有nginx.exe进程的操作
Fixed: 现在退出（Quit）能正常运行了，Reload和Restart未经测试

TODO：

更改程序META信息
添加Icon
添加关于窗口
在安装目录保存配置文件
设置nginx的配置文件位置
添加对PHP-CGI的调用操作
设置php.ini的位置
请求关闭时添加对进程的判断和Kill
跟踪建立的进程
跟踪输出的日志文件
尝试用F#重写
*添加对CoffeeScript、LESS、Sass的支持